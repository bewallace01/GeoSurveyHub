/**
 * GeoSurveyHub — Live news API keys (loaded before news-feed.js)
 * 1. Sign up free at https://rss2json.com — paste key below (required for RSS feeds).
 * 2. Optional: https://newsdata.io — paste key for extra keyword search results.
 */
window.GSH_NEWS_KEYS = {
  rss2jsonKey: 'YOUR_RSS2JSON_KEY',
  newsdataKey: '',
};
