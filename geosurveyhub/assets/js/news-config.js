/**
 * GeoSurveyHub — News API defaults (safe to commit).
 * Put real keys in news-config.local.js (gitignored) — see NEWS-SETUP.md.
 */
window.GSH_NEWS_KEYS = {
  rss2jsonKey: 'YOUR_RSS2JSON_KEY',
  newsdataKey: '',
};
